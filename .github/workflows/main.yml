name: build and deploy

on:
  pull_request:
    branches:
    - main
  workflow_dispatch:
    
jobs:
  buildAndRun:
    runs-on: ubuntu-latest
    steps:
      - name: yarn install and build
        run: |
          yarn
          yarn build
