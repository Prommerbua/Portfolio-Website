import React from 'react'
import { Container } from 'react-bootstrap'

export const ProjectNavigation = () => {
    return (
        <Container fluid className='d-flex flex-column p-0' style={{ marginTop: 'auto' }}>
            Test
        </Container>
    )
}
